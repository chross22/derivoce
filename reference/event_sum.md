# Sum a value over each labelled event

Sum a value over each labelled event

## Usage

``` r
event_sum(id, values)
```

## Arguments

- id:

  event labels, 0 outside an event

- values:

  a numeric vector

## Value

a numeric vector of the event total, broadcast to its rows
