# Default initial separation for a seeded grid

One grid cell, in km, measured at the grid's mean latitude.

## Usage

``` r
default_separation(seeds)
```

## Arguments

- seeds:

  two-column matrix of lon/lat

## Value

separation in km
