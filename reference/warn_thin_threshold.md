# Warn when the threshold rests on too few values

Warn when the threshold rests on too few values

## Usage

``` r
warn_thin_threshold(groups, percentile)
```

## Arguments

- groups:

  the climatological grouping

- percentile:

  the requested percentile

## Value

invisible `NULL`
