# Coastline geometry covering the data's extent

Coastline geometry covering the data's extent

## Usage

``` r
shoreline(env_dat, resolution = "medium", margin = 5)
```

## Arguments

- env_dat:

  an `sf` object

- resolution:

  Natural Earth resolution

- margin:

  degrees of padding around the data

## Value

an `sf` geometry of coastlines
