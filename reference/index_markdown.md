# Render the index dictionary as a markdown table

Render the index dictionary as a markdown table

## Usage

``` r
index_markdown(dictionary)
```

## Arguments

- dictionary:

  the plain data frame of entries

## Value

a single character string, newline separated
