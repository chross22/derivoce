# Declarations for units outside the EML dictionary

EML validates against a fixed dictionary of 195 units. These are the
ones this package produces that are not in it, in the shape a `unitList`
needs.

## Usage

``` r
eml_unit_registry()
```

## Value

a data frame of custom unit declarations
