# Smaller grid cell dimension, in km

Smaller grid cell dimension, in km

## Usage

``` r
grid_spacing_km(env_dat)
```

## Arguments

- env_dat:

  an `sf` POINT object on a regular grid

## Value

cell size in km
