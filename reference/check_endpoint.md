# Validate a section endpoint

Validate a section endpoint

## Usage

``` r
check_endpoint(point, label)
```

## Arguments

- point:

  a candidate `c(longitude, latitude)`

- label:

  argument name, for the message

## Value

invisibly `TRUE`
