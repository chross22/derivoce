% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shore.R
\name{distance_to_shore}
\alias{distance_to_shore}
\title{Distance to the nearest shoreline}
\usage{
distance_to_shore(
  env_dat,
  resolution = c("medium", "large", "small"),
  margin = 5,
  name = "shore_dist"
)
}
\arguments{
\item{env_dat}{an \code{sf} POINT object from \code{datamatch::accessEnvDat()}}

\item{resolution}{Natural Earth coastline resolution: \code{"medium"}, \code{"large"},
or \code{"small"}}

\item{margin}{degrees of padding around the data when cropping the coastline.
Land just outside the study area can still be the nearest shore, so cropping
tightly to the bounding box would overstate distances at the edges.}

\item{name}{name for the new column}
}
\value{
\code{env_dat} with a distance-to-shore column, in km
}
\description{
Distance from each point to the nearest coast, in kilometres. Static — it does
not vary in time — so it is computed once for the unique locations and shared
across every time step.
}
\details{
Distance from shore is a broad proxy for several things at once: depth,
terrestrial nutrient input, tidal mixing, and larval retention all covary with
it. That makes it a useful covariate and a poor explanation — a model leaning
on it is telling you \emph{where}, not \emph{why}. This is the covariate the older
pipeline had as \code{dist}.
}
\section{Choosing a coastline}{

\code{resolution} selects the Natural Earth coastline detail:

\itemize{
\item \code{"medium"} (the default, 1:50m) suits shelf-scale work.
\item \code{"large"} (1:10m) resolves individual islands and inlets, which
matters in a place like the Gulf of Maine where the coast is deeply
indented, but is slower and needs \code{rnaturalearthhires}.
\item \code{"small"} (1:110m) is too coarse for coastal work; it smooths bays
away entirely.
}

Distances are computed against true geodesic distance on the ellipsoid rather
than a projected approximation, so they stay correct across a wide domain.
}

\examples{
\dontrun{
env <- distance_to_shore(env)
# Finer coastline, for a deeply indented shore:
env <- distance_to_shore(env, resolution = "large")
}
}
