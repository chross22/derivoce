% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rolling.R
\name{rolling_covariate}
\alias{rolling_covariate}
\title{Rolling summary of a covariate over a trailing window}
\usage{
rolling_covariate(
  env_dat,
  vars = NULL,
  n = 3,
  by = c("step", "day", "month", "year"),
  stat = c("mean", "sd", "min", "max", "sum", "median", "range"),
  min_obs = 1L,
  suffix = NULL
)
}
\arguments{
\item{env_dat}{an \code{sf} POINT object from \code{datamatch::accessCopernicus()}}

\item{vars}{covariate columns, or \code{NULL} for all numeric ones}

\item{n}{length of the window, in \code{by} units, including the current step}

\item{by}{\code{"step"} to count positions in the record, or \code{"day"}, \code{"month"},
\code{"year"} to count calendar time}

\item{stat}{one or more of \code{"mean"}, \code{"sd"}, \code{"min"}, \code{"max"}, \code{"sum"},
\code{"median"}, \code{"range"}}

\item{min_obs}{fewest non-missing values a window must hold to be summarised}

\item{suffix}{one per \code{stat}, or \code{NULL} to name them automatically}
}
\value{
\code{env_dat} with one column per covariate per statistic
}
\description{
Summarises each location's recent history: the mean of the last three months,
the variability of the last year, the coldest step in the last six. Where
\code{\link[=integrate_covariate]{integrate_covariate()}} accumulates a total, this describes the distribution
the total came from, which is often the more useful covariate — a mean and a
standard deviation say different things about a place than their sum does.
}
\details{
The window is \strong{trailing and inclusive}: it ends at the current step and
includes it. A three-month mean at March covers January, February and March.
}
\section{Steps or calendar time}{

\code{by = "step"} counts positions in the record and \code{by = "day"}, \code{"month"} or
\code{"year"} count calendar time, exactly as in \code{\link[=lag_covariate]{lag_covariate()}}. The two agree
until the record has a gap and then disagree silently: on a monthly series
missing April, a three-\emph{step} window at June covers March, May and June,
while a three-\emph{month} window covers April, May and June and finds only two of
them.

Which is right depends on the question. "The mean of the last three months"
is a statement about the ocean and wants \code{by = "month"}. "The mean of the
last three observations" is a statement about the record.
}

\section{Windows that are not full}{

Early steps have less history behind them than the window asks for, and a
location can be absent from some of the steps in it. \code{min_obs} sets how many
values a window must actually contain before it is summarised; below that the
result is \code{NA} rather than a mean of whatever happened to be there. The
default of 1 is permissive, so the first steps of a record get a summary of a
short window rather than nothing. Raise it if a partial window would mislead.
}

\examples{
\dontrun{
# Conditions over the season leading up to each observation.
env <- rolling_covariate(env, "SST", n = 3, by = "month")

# How variable it has been, which is a different covariate from how warm.
env <- rolling_covariate(env, "SST", n = 12, by = "month", stat = "sd")
}
}
\seealso{
\code{\link[=integrate_covariate]{integrate_covariate()}}, \code{\link[=lag_covariate]{lag_covariate()}}, \code{\link[=cell_anomaly]{cell_anomaly()}}
}
