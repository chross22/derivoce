% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/persistence.R
\name{front_frequency}
\alias{front_frequency}
\title{How often a place is frontal}
\usage{
front_frequency(
  env_dat,
  var,
  threshold = NULL,
  quantile = 0.9,
  scope = c("record", "step"),
  per = c("km", "m"),
  n = NULL,
  by = c("step", "day", "month", "year"),
  name = NULL
)
}
\arguments{
\item{env_dat}{an \code{sf} POINT object with one row per location and time step,
as datamatch's access functions return}

\item{var}{the covariate whose gradient defines a front}

\item{threshold}{gradient magnitude at or above which a cell is frontal. When
\code{NULL}, taken from \code{quantile}}

\item{quantile}{quantile of the gradient used as the threshold}

\item{scope}{\code{"record"} for one cutoff across the series, \code{"step"} for a
cutoff per time step}

\item{per}{distance unit for the gradient, \code{"km"} or \code{"m"}}

\item{n}{length of the trailing window, or \code{NULL} for the whole record}

\item{by}{\code{"step"}, \code{"day"}, \code{"month"} or \code{"year"}, as in \code{\link[=rolling_covariate]{rolling_covariate()}}}

\item{name}{name for the new column}
}
\value{
\code{env_dat} with a frequency column between 0 and 1
}
\description{
The fraction of time steps in which a cell's own gradient was sharp enough to
count as a front. Where \code{\link[=distance_to_front]{distance_to_front()}} asks how far the nearest front
was at one moment, this asks how reliably a front sits in this place at all.
}
\details{
The distinction matters because fronts move. A cell that is frontal in one
step out of twenty happened to catch a passing filament; a cell that is
frontal in fifteen sits on a persistent feature — a shelf-break front, a tidal
mixing front, the edge of a plume — and those are the ones that aggregate
plankton reliably enough for a predator to learn. An instantaneous distance
cannot tell the two apart, and averaging distance over time does not either,
because a cell can be near a different transient front every step.
}
\section{Choosing the threshold}{

Inherited from \code{\link[=distance_to_front]{distance_to_front()}}, and the choice matters more here. With
\code{scope = "record"} one cutoff applies throughout, so frequency reflects both
how often a front is present and whether this part of the domain is gradient-
rich at all. With \code{scope = "step"} each step is cut at its own quantile, so a
fixed fraction of cells is frontal in every step and frequency becomes purely
about location. The second is usually what "persistence" is meant to mean.
}

\section{The window}{

\code{n = NULL}, the default, uses the whole record and gives a static map: one
value per cell, repeated on every step. That is a description of the domain
rather than a covariate that varies with the observation.

Giving \code{n} makes it a trailing window in the manner of \code{\link[=rolling_covariate]{rolling_covariate()}},
so it varies through the record and can enter a model alongside conditions at
the time. Steps where the cell's gradient is undefined — the outermost ring,
or a missing value — are left out of the denominator rather than counted as
not frontal.
}

\examples{
\dontrun{
# A static map of where thermal fronts persist.
env <- front_frequency(env, "SST", scope = "step")

# How frontal this place has been over the preceding year.
env <- front_frequency(env, "SST", scope = "step", n = 12, by = "month")
}
}
\seealso{
\code{\link[=distance_to_front]{distance_to_front()}}, \code{\link[=rolling_covariate]{rolling_covariate()}}
}
