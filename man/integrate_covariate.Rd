% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{integrate_covariate}
\alias{integrate_covariate}
\title{Time-integrated covariate}
\usage{
integrate_covariate(env_dat, vars = NULL, window = "year", suffix = "_int")
}
\arguments{
\item{env_dat}{an \code{sf} POINT object with one row per location and time step,
as datamatch's access functions return}

\item{vars}{covariate columns to integrate; \code{NULL} does all of them}

\item{window}{\code{"year"} to accumulate from the start of each calendar year,
\code{"all"} to accumulate over the whole record, or a positive integer for a
rolling window of that many time steps}

\item{suffix}{suffix for the new columns}
}
\value{
\code{env_dat} with an integrated column per covariate
}
\description{
Accumulates a covariate over preceding time steps at each location. A survey
does not sample the food available that instant so much as the food that has
built up since the season began, which is what the original pipeline's
\code{int_chl} captured for chlorophyll.
}
\details{
The default \code{window = "year"} reproduces \code{int_chl} of Ross et al. (2023),
which integrated chlorophyll from January: a running sum from January
of each year, reset at the year boundary. A numeric window instead sums over
that many trailing time steps, giving a rolling total that does not reset.
}
\examples{
\dontrun{
env <- integrate_covariate(env, "CHL")               # int_chl, Ross et al. 2023
env <- integrate_covariate(env, "CHL", window = 3)   # trailing 3-step total
}
}
\references{
Ross C, Runge J, Roberts J, Brady D, Tupper B, Record N (2023). Estimating
North Atlantic right whale prey based on Calanus finmarchicus thresholds.
\emph{Marine Ecology Progress Series} \strong{703}, 1-16. \doi{10.3354/meps14204}
}
