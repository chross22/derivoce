% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eddies.R
\name{distance_to_eddy}
\alias{distance_to_eddy}
\title{Distance to the nearest eddy}
\usage{
distance_to_eddy(
  env_dat,
  u = "UO",
  v = "VO",
  threshold = 0.2,
  min_cells = 4L,
  polarity = c("any", "cyclonic", "anticyclonic"),
  per = c("km", "m"),
  name = NULL
)
}
\arguments{
\item{env_dat}{an \code{sf} POINT object with one row per location and time step,
as datamatch's access functions return}

\item{u}{name of the eastward velocity column, in m/s}

\item{v}{name of the northward velocity column, in m/s}

\item{threshold}{multiple of the standard deviation of Okubo-Weiss below
which a cell counts as rotational. Positive; 0.2 follows the literature}

\item{min_cells}{smallest connected region kept, in cells}

\item{polarity}{\code{"any"}, \code{"cyclonic"}, or \code{"anticyclonic"}}

\item{per}{distance unit for the radius, \code{"km"} or \code{"m"}}

\item{name}{name for the new column}
}
\value{
\code{env_dat} with a distance column, in \code{per} units. A cell inside a
qualifying eddy has a distance of 0. A step containing no qualifying eddy
returns \code{NA} rather than a distance to nothing
}
\description{
The sibling of \code{\link[=distance_to_front]{distance_to_front()}} and \code{\link[=distance_to_isobath]{distance_to_isobath()}}. Being
outside an eddy but close to one is a different place from being far from
any, and a binary inside/outside flag cannot express it: a station 5 km from
a rotating core and one 300 km away both score zero.
}
\details{
\code{polarity} narrows it to one kind. Because cyclonic and anticyclonic eddies
do opposite things to the water column, distance to the nearest \emph{cyclonic}
eddy is often the covariate that carries a signal where distance to the
nearest eddy of any sort does not.
}
\examples{
\dontrun{
env <- distance_to_eddy(env, polarity = "cyclonic")
}
}
\seealso{
\code{\link[=detect_eddies]{detect_eddies()}}, \code{\link[=distance_to_front]{distance_to_front()}}
}
