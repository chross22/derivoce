% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indices.R
\name{derived_indices}
\alias{derived_indices}
\alias{print.derivoce_index_table}
\title{Regional indices this package can derive}
\usage{
derived_indices(markdown = FALSE)

\method{print}{derivoce_index_table}(x, ...)
}
\arguments{
\item{markdown}{return a markdown table instead of printing a formatted one,
for pasting into documentation}

\item{x}{a \code{derivoce_index_table}}

\item{...}{ignored}
}
\value{
a data frame of class \code{derivoce_index_table}, or a character string of
markdown when \code{markdown = TRUE}
}
\description{
The named region-scale indices, what each measures, what it needs as input,
and where the concept comes from. All are derived from gridded fields rather
than downloaded, which distinguishes them from the climate indices
\code{datamatch::attach_climate_index()} serves.
}
\section{Why three ways of measuring the same thing}{

Scotian Shelf inflow to the Gulf of Maine appears here three times, because
the literature measures it three ways and they answer different questions.

A \strong{transport} across a section measures the crossing itself, and is the only
one that gives a direction and a flux. A \strong{water-mass fraction} measures how
much of the water present came from there, which is what matters for nutrients
and is available even where velocities are not. A \strong{box anomaly} measures the
most visible consequence, freshening, and is the most robust to poor velocity
data but the least specific about cause.

They disagree in informative ways. A strong inflow with a normal salinity
anomaly means the arriving water was not unusually fresh, which is a real
finding about the upstream shelf rather than a contradiction.
}

\examples{
derived_indices()

cat(derived_indices(markdown = TRUE))
}
\seealso{
\code{\link[=scotian_shelf_inflow]{scotian_shelf_inflow()}}, \code{\link[=northeast_channel_inflow]{northeast_channel_inflow()}},
\code{\link[=water_mass_fraction]{water_mass_fraction()}}, \code{\link[=eastern_gom_salinity]{eastern_gom_salinity()}}
}
