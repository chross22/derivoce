% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deformation.R
\name{flow_deformation}
\alias{flow_deformation}
\title{Velocity gradient diagnostics: vorticity, divergence, strain, Okubo-Weiss}
\usage{
flow_deformation(
  env_dat,
  u = "UO",
  v = "VO",
  measures = c("vorticity", "divergence", "strain_rate", "okubo_weiss"),
  suffix = ""
)
}
\arguments{
\item{env_dat}{an \code{sf} POINT object from \code{datamatch::accessCopernicus()}}

\item{u}{name of the eastward velocity column, in m/s}

\item{v}{name of the northward velocity column, in m/s}

\item{measures}{which diagnostics to add. Any of \code{"vorticity"},
\code{"divergence"}, \code{"normal_strain"}, \code{"shear_strain"}, \code{"strain_rate"},
\code{"okubo_weiss"} and \code{"rossby"}}

\item{suffix}{appended to each measure to name its column}
}
\value{
\code{env_dat} with one column per requested measure. Vorticity,
divergence and the strains are in s^-1, Okubo-Weiss in s^-2, and the Rossby
number is dimensionless
}
\description{
Derives the local structure of a flow from the four horizontal derivatives of
its velocity components. Together these say whether a parcel of water is
spinning, converging, being pulled apart, or some combination.
}
\details{
All are instantaneous and local: they describe the flow in one cell at one
time step, and need no history. That places them between \code{\link[=eke]{eke()}}, which needs
a series to form an anomaly, and \code{\link[=ftle]{ftle()}} and \code{\link[=fsle]{fsle()}}, which advect particles
through many steps. Where those two answer "how variable is this place" and
"where do trajectories separate", these answer "what is the flow doing here,
right now".
}
\section{The measures}{

Writing the derivatives of eastward \code{u} and northward \code{v} with respect to
eastward \code{x} and northward \code{y}:
\itemize{
\item \strong{vorticity}, \eqn{\zeta = \partial v/\partial x - \partial u/\partial y},
is rotation. Positive is counter-clockwise (cyclonic in the northern
hemisphere), which in an eddy means upwelling at its core.
\item \strong{divergence}, \eqn{\partial u/\partial x + \partial v/\partial y}, is
spreading. Negative is convergence, where surface water piles up and sinks,
and where buoyant particles and floating material accumulate.
\item \strong{normal_strain}, \eqn{\partial u/\partial x - \partial v/\partial y}, and
\strong{shear_strain}, \eqn{\partial v/\partial x + \partial u/\partial y},
stretch a parcel along the axes and diagonally.
\item \strong{strain_rate} is their magnitude, the total rate of deformation.
\item \strong{okubo_weiss}, \eqn{W = S_n^2 + S_s^2 - \zeta^2}, compares the two.
Negative means rotation wins, which is the interior of a coherent eddy.
Positive means strain wins, which is the filaments between eddies where
water is drawn out into long thin structures.
\item \strong{rossby} is \eqn{\zeta/f}, vorticity as a fraction of planetary rotation.
It is the dimensionless version, so it can be compared between latitudes,
and values approaching 1 mean the flow is fast enough for the usual
geostrophic balance to be breaking down.
}
}

\section{What it cannot tell you}{

These are single-time-step quantities, so a large value means the flow is
deforming now, not that it has been or will be. A coherent eddy that persists
for weeks and a momentary filament can carry the same Okubo-Weiss value.
Persistence is what \code{\link[=ftle]{ftle()}} and \code{\link[=fsle]{fsle()}} measure, at much greater cost.

The derivatives are central differences, so like every gradient in this
package the outermost ring of cells is \code{NA}, and the values are only as
smooth as the grid. On a coarse product a real eddy smaller than a few cells
will not appear at all.
}

\examples{
\dontrun{
env <- flow_deformation(env)

# Eddy interiors are where rotation beats strain.
eddy_core <- env$okubo_weiss < 0
}
}
\references{
Okubo A (1970). Horizontal dispersion of floatable particles in the vicinity
of velocity singularities such as convergences. \emph{Deep-Sea Research and
Oceanographic Abstracts} \strong{17}(3), 445-454.
\doi{10.1016/0011-7471(70)90059-8}

Weiss J (1991). The dynamics of enstrophy transfer in two-dimensional
hydrodynamics. \emph{Physica D: Nonlinear Phenomena} \strong{48}(2-3), 273-294.
\doi{10.1016/0167-2789(91)90088-Q}

Isern-Fontanet J, Garcia-Ladona E, Font J (2003). Identification of marine
eddies from altimetric maps. \emph{Journal of Atmospheric and Oceanic Technology}
\strong{20}(5), 772-778.
\doi{10.1175/1520-0426(2003)20<772:IOMEFA>2.0.CO;2}
}
\seealso{
\code{\link[=eke]{eke()}}, \code{\link[=ftle]{ftle()}}, \code{\link[=fsle]{fsle()}}, \code{\link[=horizontal_gradient]{horizontal_gradient()}}
}
