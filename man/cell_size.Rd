% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gradient.R
\name{cell_size}
\alias{cell_size}
\title{Cell size in real distance units}
\usage{
cell_size(layer, per = "km")
}
\arguments{
\item{layer}{a \code{SpatRaster}}

\item{per}{\code{"km"} or \code{"m"}}
}
\value{
\code{list(x =, y =)}, where \code{x} is a \code{SpatRaster} of per-row longitude
spacing and \code{y} a scalar
}
\description{
Converts a lon/lat grid's degree spacing into distance. Longitude spacing
shrinks with the cosine of latitude, so it is computed per row rather than
once for the whole grid — at 45 degrees the error from ignoring that is about
30 percent.
}
\details{
A projected raster is already in linear units, so its resolution is used
directly.
}
\keyword{internal}
