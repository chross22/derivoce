.Rproj.user
.Rhistory
.RData
.Ruserdata
.DS_Store

# knitr scratch output from rendering a vignette outside R CMD build
/figure/
# R CMD build and check leftovers
*.Rcheck/
*.tar.gz
# generated by R CMD build, not source
/inst/doc/
/build/

# LCR pipeline: velocity downloads and trajectory output are large and regenerable.
# Unanchored so that subdirectories of data/ are covered too: these are hundreds
# of megabytes each and a stray one committed is not easily undone.
glorys_*.nc
/lcr-pipeline/output/
__pycache__/
.pytest_cache/

# generated pkgdown site
pkgdown-site/
